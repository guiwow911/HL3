# 部署到公网 · 操作手册

> 游戏是**纯静态**的（HTML + JS + 一个 Three.js 文件，共约 1MB），
> 不需要后端、不需要数据库、没有任何外部依赖 —— 任何静态托管都能跑。

---

## 方案对比

| 方案 | 地址 | 需要账号 | 生效速度 | 适合 |
|---|---|---|---|---|
| **A. GitHub Pages** | `https://用户名.github.io/half-life-3/` | GitHub 账号 | 1~2 分钟 | **长期分享（推荐）** |
| **B. Cloudflare Pages** | `https://xxx.pages.dev` | Cloudflare 账号 | 1~2 分钟 | 长期分享，国内访问通常更快 |
| **C. 临时公网隧道** | `https://xxx.trycloudflare.com` | **不需要** | 立刻 | 马上给朋友试玩 |
| **D. 自己的服务器** | 你的域名 | 服务器 | 看配置 | 有 VPS / 域名 |

---

## 方案 A：GitHub Pages（推荐，免费永久）

### 一键脚本
双击 **`部署到公网.bat`** → 选 `2`。脚本会自动：
1. `git init` 初始化仓库
2. 提交全部文件
3. 打印接下来要粘贴的两行命令

### 手动步骤
```bash
cd Half-Life-3
git init
git branch -M main
git add -A
git commit -m "半条命 3：重返黑山"
```

然后在浏览器里：
1. 打开 <https://github.com/new>
2. 仓库名填 `half-life-3`，可见性选 **Public**
   ⚠️ **不要**勾选 Add README / .gitignore（必须是空仓库）
3. 创建后回命令行执行（把 `你的用户名` 换掉）：
```bash
git remote add origin https://github.com/你的用户名/half-life-3.git
git push -u origin main
```
4. 推送完成后，进入仓库 **Settings → Pages**
   - Source 选 **Deploy from a branch**
   - Branch 选 **main**，目录选 **/(root)**，保存
5. 等 1~2 分钟，访问：
   ```
   https://你的用户名.github.io/half-life-3/
   ```

**以后更新游戏**：改完文件后执行
```bash
git add -A
git commit -m "更新"
git push
```
等 1 分钟左右自动生效。

> 仓库里已经放了 `.nojekyll`，避免 GitHub 的 Jekyll 处理干扰。

---

## 方案 B：Cloudflare Pages（免费、国内速度通常更好）

1. 打开 <https://dash.cloudflare.com/> 注册/登录（免费）
2. 左侧 **Workers & Pages → Create → Pages → Upload assets**
3. 项目名随便填，例如 `half-life-3`
4. 把 `Half-Life-3` 文件夹里的**全部文件**（含 `js/`、`vendor/` 两个文件夹）拖进上传区
   ⚠️ 注意要包含子文件夹，不要只拖 index.html
5. 点 **Deploy**，几十秒后得到 `https://half-life-3.pages.dev`

仓库里已经放了 `_headers` 文件，Cloudflare Pages 会自动读取，把 `vendor/three.min.js` 设成长期缓存。

**无需 GitHub，纯拖拽即可。**

---

## 方案 C：临时公网隧道（不需要任何账号，立刻可玩）

双击 **`部署到公网.bat`** → 选 `1`，或者直接在本目录执行：

```bash
node tunnel.js
```

脚本会：
1. 启动本地服务器 `node server.js`
2. 用 **Windows 自带的 ssh** 建一条反向隧道到 `localhost.run`（**零下载、零注册**，约 5 秒建立）
3. 打印形如 `https://xxxxxxxx.lhr.life` 的公网地址，**并自动复制到剪贴板**
4. 自动从公网回连验证（返回 200 且校验字节数）后才告诉你"验证通过"

把这个地址发给朋友，他们打开浏览器就能玩。手机打开会自动切成触屏操作。

**实测结果**
```
[index.html]        200   28,566 B  text/html
[js/game.js]        200  125,712 B  application/javascript
[vendor/three.min.js] 200 603,445 B application/javascript
[manifest.webmanifest] 200    698 B  application/manifest+json
→ 12 个资源全部 200，0 失败
```

**注意事项**
- 地址是**临时的**：关掉命令行窗口、或电脑关机/休眠，地址立即失效
- 每次启动会分配一个**新的随机子域名**（匿名隧道没有固定域名）
- 别人访问的流量走你的网络，人多了会卡
- 隧道刚建立的头十几秒可能还不通，刷新一下即可
- 想要长期稳定地址，请用方案 A / B

> `tunnel.js` 也支持 Cloudflare 隧道作为备选：如果检测到同目录下有 `cloudflared.exe`，
> 且 ssh 不可用时会自动改用它（`https://xxx.trycloudflare.com`）。
> 但 cloudflared 安装包有 52MB，网络不好时不建议。

---

## 方案 D：部署到自己的服务器 / VPS

把整个 `Half-Life-3` 目录上传到服务器，例如 `/var/www/half-life-3/`。

**Nginx 配置**
```nginx
server {
    listen 80;
    server_name 你的域名.com;

    root /var/www/half-life-3;
    index index.html;

    # three.min.js 永不变化，长期缓存；其余文件不缓存，方便更新
    location = /vendor/three.min.js {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    gzip on;
    gzip_types application/javascript text/css text/html application/manifest+json;
    gzip_min_length 1024;
}
```

**或者直接用自带的 Node 服务器**（已监听 `0.0.0.0`）
```bash
node server.js          # 默认 8123 端口
PORT=80 node server.js  # 换端口
```
配合 pm2 常驻：
```bash
npm i -g pm2
pm2 start server.js --name hl3
pm2 save
```

**⚠️ 建议配 HTTPS**：`screen.orientation.lock()`（手机横屏锁定）和全屏 API 在 HTTPS 下才可靠。
用 `certbot` 申请免费证书，或套一层 Cloudflare 代理。

---

## 共享前须知

- **这是个粉丝同人作品**：《半条命》相关版权归 Valve 所有，本项目未使用其任何美术/音频素材（图形与声音 100% 由代码生成）。
  公开分享建议保留 `README.md` 里的声明，且**不要用于商业用途**。
- 首次打开会有约 1 秒的程序化贴图生成过程（会显示"正在构建黑山基地…"），属正常现象。
- 手机端会自动识别为触屏操作；如果不准，主菜单「操作方式」可以手动切换。

---

## 常见问题

**打开是 404 / 白屏**
- GitHub Pages：确认仓库根目录直接有 `index.html`（不是套了一层文件夹）
- Cloudflare 拖拽上传：确认 `js/` 和 `vendor/` 两个文件夹也上传了

**能打开但一直停在"正在构建黑山基地…"**
- 说明 WebGL 初始化失败。手机浏览器请关闭"省流量/极速模式"（这类模式会阉割 WebGL），
  或换 Chrome / Safari 打开。

**鼠标不能转视角**
- 电脑端必须点击页面里的「开始游戏」按钮（浏览器规定锁定鼠标必须由用户操作触发），然后点弹窗的"允许"。

**推送 GitHub 时要求登录**
- 首次 `git push` 会弹出登录窗口，选择"浏览器授权"即可；
  或到 GitHub → Settings → Developer settings → Personal access tokens 生成一个 token 当作密码用。
